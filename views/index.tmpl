{{ define "title" }}Hello, world.{{ end }}

{{ define "content" }}
{{if .Email }}
<p>You are logged in as {{.Email}}</p>
{{ else }}
<p>You are not logged in. <a href="/login">Log in</a> now, or <a href="sign-in">sign up</a>.</p>
{{ end }}
{{ end }}